<UserControl x:Class="DashboardTienda.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:uc="clr-namespace:DashboardTienda.UserControls"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:DashboardTienda">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="130"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <!-- Info Card -->
        <Grid Margin="10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>


            <!-- Card -->
            <uc:InfoCard Grid.Column="0" Grid.Row="0" Title="Sales" SubTitle="Compared to ($2646 Last Month)" Amount="3" Image="/Assets/sale.png"/>
            <uc:InfoCard Grid.Column="1" Grid.Row="0" Title="Purchase" SubTitle="Compared to ($1236 Last Month)" Amount="3" Image="/Assets/purchase.png"/>
            <uc:InfoCard Grid.Column="2" Grid.Row="0" Title="Return" SubTitle="Compared to (1422 Last Month)" Amount="3" Image="/Assets/return.png"/>
        </Grid>

        <!-- Main Chart Section -->
        <Border Style="{DynamicResource whiteBorder}" Grid.Row="1" Margin="20 20 20 0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="15 5" Orientation="Vertical">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Productos" Style="{DynamicResource titleText}" VerticalAlignment="Center"/>
                        <Button Click="RefreshUsers" Style="{DynamicResource menuButton}">
                            <Icon:PackIconMaterial Padding="1" Margin="10 0" Kind="Refresh" Foreground="{DynamicResource HighlightBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        </Button>
                    </StackPanel>
                    <StackPanel x:Name="ProductsPanel" Margin="15 5" Orientation="Vertical">

                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

        </Border>

    </Grid>
</UserControl>
